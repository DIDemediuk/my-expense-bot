# 🔍 АНАЛІЗ БОТА: ПРОБЛЕМИ ТА ВИПРАВЛЕННЯ

## 📋 РЕЗЮМЕ ПРОБЛЕМ
Бот має **серйозні архітектурні проблеми**, що перешкоджають правильній роботі вибору категорій, збереженню даних та обробці сум.

---

## 🔴 КРИТИЧНІ ПРОБЛЕМИ

### 1️⃣ **Інконсистентна структура ConversationHandler'ів**
**Проблема:** Бот імпортує хендлери з різних місць, але обробники не пов'язані правильно.

**Де:** `bot.py` (lines 22-60)
```python
# ❌ ПРОБЛЕМА: Хендлери з expense_handler.py не переповідні до expense_conv
conv_handler = ConversationHandler(
    entry_points=[CallbackQueryHandler(handle_callback)],  # ❌ Занадто широкий фільтр
    states={
        WAITING_SIMPLE_PERIOD: [CallbackQueryHandler(handle_simple_period)],  # ❌ Ці функції не існують!
    },
    fallbacks=[],
)
```

**Наслідок:** Стани не переходять правильно, користувач застрягає в одному місці.

---

### 2️⃣ **Неправильна обробка категорій і підкатегорій**
**Проблема:** `expense_handler.py` має функції-«пустинки» для всіх кроків:
```python
async def handle_simple_date(update, context):
    pass

async def handle_simple_period(update, context):
    pass
```

**Наслідок:** Користувач не отримує меню для вибору категорій. Крок просто спускається або зависає.

**Де:** `handlers/simplified_expense.py` (lines 18-40)

---

### 3️⃣ **Подвійна реалізація логіки в різних файлах**
**Проблема:** Вся логіка добре реалізована в `main.py`, але недоступна з `bot.py` та `conversation.py`.

**Приклад:**
- `main.py` має `handle_callback()` з повною логікою (lines 170-450)
- `bot.py` імпортує `handle_callback()` з `main_handler.py`
- `handlers/main_handler.py` передбачає `ask_expense_date()` з `expense_handler.py`

**Наслідок:** Циклічні імпорти, конфлікти, функції не викликаються.

---

### 4️⃣ **Помилкова обробка суми у Google Sheets**
**Проблема:** В `sheets.py` функція `add_expense_to_sheet()` має помилку в колонках для `other`:

```python
row = [
    date_str,
    "Розхід",
    parsed["рахунок"],
    period,
    location,
    category_vitrat,        # ❌ ПОРОЖНЯ ЗМІННА!
    change,
    category,
    subcategory,
    subsubcategory,
    parsed["сума"],
    parsed["коментар"] or ""
]
```

**Наслідок:** Сума може записатися в неправильну колонку або перезаписати відгомін.

**Де:** `sheets.py` (lines 71-95)

---

### 5️⃣ **Парсер суми неправильно обробляє повідомлення користувача**
**Проблема:** `parse_expense_simple()` очікує формат: `ФОП №X СУМА ОПИС`
Але в `main_handler.py` просимо формат: `СУМА ОПИС`

```python
# main_handler.py
await query.message.edit_text(
    "✅ **Dividends**\n\n📝 Введіть: `СУМА ФОП Ім'я` (напр. `2000 ФОП2 Ваня`):",
    parse_mode='Markdown'
)
```

**Наслідок:** Користувач вводить неправильний формат, сума не розпізнається.

**Де:** `handlers/main_handler.py` (line 97), `sheets.py` (lines 54-66)

---

## 🟡 ВИСОКИЙ ПРІОРИТЕТ: Проблеми з обробкою категорій

### 6️⃣ **Вибір категорій/підкатегорій не збережується**
**Проблема:** У функції `add_expense_to_sheet()` використовуються дані з `context.user_data`, але вони не заповнюються правильно:

```python
category_vitrat = ''  # ❌ Завжди порожня!
subcategory = context_data.get('subcategory', '')  # Може не бути встановлено
```

**Де:** `sheets.py` (lines 71-95), `handlers/expense_handler.py` (lines 81-100)

**Наслідок:** У Google Sheets колонки `Категорія витрат` і `Дод. категорії` залишаються порожніми.

---

### 7️⃣ **Динамічні категорії за локацією не працюють**
**Проблема:** У `_show_category_menu()` логіка вибору категорій складна та помилкова:

```python
# ❌ Неправильна логіка: категорії можуть не завантажитися
categories_list = CONFIG_OTHER['categories_by_change'].get(change_name, [])
if not categories_list:
    categories_dict = CONFIG_OTHER['categories_by_location'].get(location_key, {})
```

**Наслідок:** Для деяких комбінацій період/локація меню категорій не з'являється.

**Де:** `handlers/expense_handler.py` (lines 320-350)

---

## 🔵 РЕКОМЕНДАЦІЇ ДЛЯ ВИПРАВЛЕННЯ

### ✅ Крок 1: Уніфікувати структуру ConversationHandler
**Файл:** `conversation.py` (потрібно переписати)

```python
from handlers.main_handler import handle_callback
from handlers.expense_handler import (
    handle_period_selection,
    handle_location_selection,
    handle_change_selection,
    handle_category_selection,
    handle_subcategory_selection,
    handle_subsubcategory_selection,
    process_expense_input
)

# Один глобальний ConversationHandler для витрат
expense_conv = ConversationHandler(
    entry_points=[CallbackQueryHandler(handle_callback, pattern="^add_expense$")],
    states={
        WAITING_PERIOD: [CallbackQueryHandler(handle_period_selection)],
        WAITING_LOCATION: [CallbackQueryHandler(handle_location_selection)],
        WAITING_CHANGE: [CallbackQueryHandler(handle_change_selection)],
        WAITING_CATEGORY: [CallbackQueryHandler(handle_category_selection)],
        WAITING_SUBCATEGORY: [CallbackQueryHandler(handle_subcategory_selection)],
        WAITING_SUBSUBCATEGORY: [CallbackQueryHandler(handle_subsubcategory_selection)],
        WAITING_EXPENSE_INPUT: [MessageHandler(filters.TEXT & ~filters.COMMAND, process_expense_input)],
    },
    fallbacks=[CommandHandler('start', start)],
    per_chat=True,
)
```

---

### ✅ Крок 2: Заповнити порожні хендлери в `expense_handler.py`
Замінити `pass` на реальну логіку:

```python
async def handle_simple_period(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Вибір періоду для витрати типу 'other'"""
    period_key = update.callback_query.data.split('_')[-1]
    context.user_data['period'] = CONFIG_OTHER['periods'][period_key]
    # ... наступний крок меню ...
```

---

### ✅ Крок 3: Исправити формат введення суми

**Для Dividends:** Формат має бути `СУМА ФОП ІМ'Я`, але парсер чекає `ФОП СУМА`.

Переписати `parse_expense()`:
```python
def parse_expense(text: str):
    pattern = r"^(\d+(?:[ ,]\d{3})*(?:\.\d+)?)\s+(ФОП|ГОТІВКА)\s+(.+?)\s+(.*)$"
    # Тепер чекаємо: СУМА ФОП2 Ваня
```

---

### ✅ Крок 4: Виправити запис у Google Sheets
**Файл:** `sheets.py` (lines 71-95)

```python
row = [
    date_str,
    "Розхід",
    parsed["рахунок"],
    period,
    location,
    "",  # category_vitrat — залишити порожною (якщо не потрібна)
    change,
    category,
    subcategory,
    subsubcategory,
    parsed["сума"],  # ✅ СУМА — точно в правильній колонці!
    parsed["коментар"] or "",
    "Факт"  # За замовчуванням
]
```

---

### ✅ Крок 5: Перевірити колонки в Google Sheets
**Очікувані колонки для "ShiftExpenses":**
1. Дата
2. Група
3. Рахунок
4. Період
5. Локація
6. Категорія витрат
7. Зміни
8. Категорії
9. Дод. категорії
10. Дод. інформація
11. **Сума** ← ТУТ!
12. Коментар
13. Факт / Прогноз

**Переконайтесь, що в коді строго дотримуватися цього порядку:**
```python
# У add_expense_to_sheet()
row = [date_str, "Розхід", рахунок, період, локація, "", зміна, категорія, підкат, "", СУМА, коментар, "Факт"]
```

---

## 🎯 ЧЕКЛИСТ ВИПРАВЛЕНЬ

- [ ] Переписати `conversation.py` з правильною структурою
- [ ] Заповнити функції в `handlers/expense_handler.py`
- [ ] Переписати `parse_expense()` для формату `СУМА ФОП`
- [ ] Виправити колонки у `add_expense_to_sheet()`
- [ ] Протестувати введення суми з різними форматами
- [ ] Перевірити, що категорії/підкатегорії зберігаються в `context.user_data`
- [ ] Протестувати запис у Google Sheets на всіх комбінаціях період/локація/категорія

---

## 📞 ЗАПИТАННЯ ДЛЯ УТОЧНЕННЯ

1. **Який точний порядок колонок у "ShiftExpenses"?** (Перевіримо спеціально)
2. **Яких форматів суми очікуєте від користувачів?** (Тільки цифри? З "грн"?)
3. **Чи потрібна колонка "Категорія витрат"?** (Зараз там завжди порожня)

---

## 🚀 ЗАПРОПОНОВАНЕ РІШЕННЯ

Я готовий виправити:
1. **Першочергово:** Переписати `conversation.py` та заповнити хендлери
2. **Потім:** Виправити парсер суми та запис у Sheets
3. **Тестування:** Перевірити кінцево-кінцево на всіх сценаріях

Хочеш, щоб я почав?
